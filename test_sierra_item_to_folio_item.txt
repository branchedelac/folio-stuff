//BARCODE
message = 'Should add volume (varfield v) to volume'
  let data = item_volume.json
    expect permanentLocationID = '2019'

//VOLUME
message = 'Should add volume (varfield v) to volume'
  let data = item_volume.json
    expect permanentLocationID = '2019'